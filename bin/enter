#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
. "$DIR/bash.conf"

docker run -it --name simplesecrets-enter -v "${VOLUME_VCMD}" "${CONTAINER_NAME}" /bin/bash
docker rm simplesecrets-enter  > /dev/null
